export default class Icon {
  constructor(id, group, title, iconUrl, previousIconUrl) {
    this.id = id;
    this.group = group;
    this.title = title;
    this.iconUrl = iconUrl;
    this.previousIconUrl = previousIconUrl;
  }
}
