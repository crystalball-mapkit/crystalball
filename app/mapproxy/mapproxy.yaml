services:
  demo: ~

layers:
  - name: test
    title: Test Layer
    sources: [test_cache]

sources:
  test_source:
    type: tile
    url: https://a.tile.openstreetmap.org/%(z)s/%(x)s/%(y)s.png

caches:
  test_cache:
    sources: [test_source]
    grids: [webmercator]

grids:
  webmercator:
    base: GLOBAL_WEBMERCATOR

globals:
  cache:
    base_dir: './cache_data'
    lock_dir: './cache_data/locks'
